<section>
    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Delivery Address</h5>
                <h6 class="mt-3">{{orderDetails.[0].deliveryDetails.firstName}}
                    {{orderDetails.[0].deliveryDetails.lastName}} </h6>
                <p class="card-text">
                    {{orderDetails.[0].deliveryDetails.address}}-{{orderDetails.[0].deliveryDetails.pincode}}


                <form action="/invoice" method="post">
                    <input type="text" name="orderId" value="{{orderDetails.[0]._id}}" hidden>
                    <input type="text" name="proId" value="{{orderDetails.[0].product._id}}" hidden>
                    <button class="btn btn-dark float-end" type="submit">Invoice</button>
                </form>
                {{!-- <a href="/invoice/{{orderDetails.[0]._id}}" class="btn btn-dark float-end" type="submit">Invoice
                    <input type="text" name="proId" value="{{orderDetails.[0].product._id}}" hidden>
                </a> --}}

                </p>
                <p>Phone Number {{orderDetails.[0].deliveryDetails.mobileNumber}}</p>

            </div>
        </div>
    </div>

    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <a href="/viewSingleProductDetails/{{orderDetails.[0].product._id}}">
                    <div class="d-inline-flex"><img class="shadow p-3 mt-2 rounded" style="width: 140px; height: 140px;"
                            src="/product-images/{{orderDetails.[0].product.images.[0].filename}}" alt="">
                        <p class="p-4">{{orderDetails.[0].product.title}} <br> {{orderDetails.[0].product.brand}} <br>
                            <br>
                            ₹
                            {{orderDetails.[0].product.price}}
                        </p>
                        <p class="text-success p-5 mt-3 ">{{orderDetails.[0].deliveryStatus}} </p>
                    </div>
                </a>
            </div>
        </div>
    </div>
    </div>

</section>